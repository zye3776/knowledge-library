#!/bin/sh

# Load API key from .env.local if it exists (not committed to git)
[ -f .env.local ] && export $(grep -v '^#' .env.local | xargs)

# Run the pre-commit index generator script
# All logic is handled in the TypeScript script
bun scripts/pre-commit-index.ts
